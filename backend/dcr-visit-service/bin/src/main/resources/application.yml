server:
  port: 8083

spring:
  application:
    name: dcr-visit-service
  datasource:
    url: jdbc:postgresql://localhost:5432/dcr_db
    username: alleviare_admin
    password: AlleviarePassword123!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
